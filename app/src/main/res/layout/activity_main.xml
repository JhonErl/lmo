<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:custom="http://schemas.android.com/apk/res-auto"
    android:background="#aaaa"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.lockmeout1.johnerl.lmo.MainActivity">

  <TextView
      android:id="@+id/itro"
      android:textSize="24sp"
      android:textStyle="bold"
      android:textColor="#000000"
      android:text="@string/timer_set"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content" />

  <LinearLayout
  android:layout_below="@+id/itro"
    android:id="@+id/timerLayout"
    android:orientation="horizontal"
    android:paddingTop="8dp"
    android:paddingLeft="50dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

  <com.lockmeout1.johnerl.lmo.Timerset
      android:id="@+id/HTimerset"
      android:background="@drawable/border"
      android:paddingTop="10dp"
      android:layout_gravity="center"
      android:orientation="vertical"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"/>

  <com.lockmeout1.johnerl.lmo.Timerset
      android:id="@+id/MTimerset"
      android:paddingTop="10dp"
      android:background="@drawable/border"
      android:textAlignment="center"
      android:orientation="vertical"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"/>

  <com.lockmeout1.johnerl.lmo.Timerset
      android:id="@+id/STimerset"
      android:paddingTop="10dp"
      android:background="@drawable/border"
      android:textAlignment="center"
      android:orientation="vertical"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"/>
</LinearLayout>

  <Button
      android:layout_below="@+id/timerLayout"
      android:paddingTop="10dp"
      android:id="@+id/sampleButton"
      android:text="S U B M I T"
      android:textSize="27sp"
      android:textStyle="bold"
      android:textColor="#000000"
      android:layout_centerInParent="true"
      android:layout_width="match_parent"
      android:layout_height="wrap_content" />
</RelativeLayout>
